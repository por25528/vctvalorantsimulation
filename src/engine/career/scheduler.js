/**
 * engine/career/scheduler.js — builds the ordered 8-event calendar × 4 leagues,
 * wiring qualification feeds (Kickoff→M0→S1→M1→S2→M2→S3→Champions).
 * Phase 4 (Full calendar).
 */

/**
 * Build the full season calendar.
 * @param {object} world { leagues, teams, players }
 * @param {object} opts  { season:number }
 * @returns {object[]} ordered calendar events
 */
export function buildCalendar(world, opts) {
  throw new Error('not implemented: engine/career/scheduler.js');
}
