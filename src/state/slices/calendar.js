/**
 * state/slices/calendar.js — reducer slice for the season calendar/cursor.
 * Phase 3+. Pure reducer (state, action) -> new state.
 */

/** @type {object} */
export const initialCalendarState = (() => {
  throw new Error('not implemented: state/slices/calendar.js');
})();

/**
 * @param {object} state
 * @param {object} action
 * @returns {object}
 */
export function calendarReducer(state, action) {
  throw new Error('not implemented: state/slices/calendar.js');
}
