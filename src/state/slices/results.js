/**
 * state/slices/results.js — reducer slice for stored match logs (Series results).
 * Phase 3+. Pure reducer (state, action) -> new state.
 */

/** @type {object} */
export const initialResultsState = (() => {
  throw new Error('not implemented: state/slices/results.js');
})();

/**
 * @param {object} state
 * @param {object} action
 * @returns {object}
 */
export function resultsReducer(state, action) {
  throw new Error('not implemented: state/slices/results.js');
}
