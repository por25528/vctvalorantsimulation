/**
 * state/middleware/logger.js — dev logging middleware (action + state diff).
 * Phase 3.
 */

/**
 * Create the logger middleware.
 * @returns {(store:object) => (next:Function) => (action:object) => *}
 */
export function createLoggerMiddleware() {
  throw new Error('not implemented: state/middleware/logger.js');
}
