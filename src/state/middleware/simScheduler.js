/**
 * state/middleware/simScheduler.js — routes heavy simulation to the Web Worker
 * and streams results back as actions.
 * Phase 3+.
 */

/**
 * Create the sim-scheduler middleware.
 * @param {object} deps { worker }
 * @returns {(store:object) => (next:Function) => (action:object) => *}
 */
export function createSimSchedulerMiddleware(deps) {
  throw new Error('not implemented: state/middleware/simScheduler.js');
}
